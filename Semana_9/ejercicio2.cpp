/*
Ejercicio 2

Comprobar si una palabra es palindrome utilizando pilas

Para esto creará la función "bool esPalindrome(string palabra)"", la cual recibe una palabra. 
La almacena caracter por caracter en una Pila.
Posteriormente comprueba si es palindrome haciendo uso de la Pila,
si la Pila está vacía significa que la palabra era palindrome.

En el main sólo deben leer una palabra y comprobar si es palindrome llamando a la función "bool esPalindrome(char* palabra)"

PD: Son libres de elegir como quieren leer la palabra, pueden modificar la delcaracion de la función esPalindrome dependiendo de sus necesidades.
    Si no quieren utilizar la clase pila creada en el ejercicio 1 pueden utilizar <Stack> de la STL
*/

#include <iostream>
#include "Pila.h"

bool esPalindrome(string palabra);

int main(int argc, char** argv) {

}

bool esPalindrome(string palabra) {
    return false;
}